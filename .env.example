# ============================================================
# Recovery Companion — Environment Variables
# ============================================================
# This app currently has no required runtime environment variables.
# It stores recovery data locally on the device and does not call external APIs.
